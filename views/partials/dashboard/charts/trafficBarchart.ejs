<!-- <div class="col-12">
<canvas id="trafficBarchart" style="position: relative; height:400px !important; width:80vw"> </canvas>
</div> -->
<div class="col-md-6 col-xxl-6 d-flex order-2 order-xxl-6">
  <div class="card flex-fill w-100">
    <div class="px-4 pt-3">
      <h5 class="card-title mb-0">Daily Traffic Chart</h5>
  </div>
  <div class="px-4 pb-4 d-flex">
    <div class="align-self-center w-100">
      <div class="py-3">
        <div class="chart chart-xs">
          <canvas id="trafficBarchart" ></canvas>
        </div>
      </div>
    </div>
  </div>
</div>
</div>
<script>
const tBr = document.getElementById('trafficBarchart')
  const data = {
    labels: ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday', 'Sunday'],
    datasets: [
      {
        label: 'Daily Traffic',
        data: [200, 300, 400, 250, 350, 500, 450],
        backgroundColor: '#87CEEB',
        borderColor: '#AFEEEE',
        borderWidth: 3,
      }
    ]
  }
  const options = {
    scales: {
      y: {
        beginAtZero: true
      }
    }
  }
  const myChart = new Chart(tBr, {
    type: 'bar',
    data: data,
    options: options
  })
</script>